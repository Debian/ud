% for i, group in enumerate(instance.groups):
0${i} ${group.gid}:x:${group.gidNumber}:${ ','.join([x.uid for x in group.hid2users[instance.hid]]) }
.${group.gid} ${group.gid}:x:${group.gidNumber}:${ ','.join([x.uid for x in group.hid2users[instance.hid]]) }
=${group.gidNumber} ${group.gid}:x:${group.gidNumber}:${ ','.join([x.uid for x in group.hid2users[instance.hid]]) }
% endfor
